import numpy as np

arr = np.array([0, np.pi/2, np.pi])

# -----------------------------------
# Trigonometric functions
# -----------------------------------

print(np.sin(arr))
# sin values

print(np.cos(arr))
# cos values

print(np.tan(arr))
# tan values

# note:
# angles must be in radians (not degree)